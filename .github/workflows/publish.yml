name: Publish to npm

on:
  push:
    tags:
      - 'tasksy-v*.*.*'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm typecheck

      - run: pnpm test

      - run: pnpm build

      # pnpm does not perform npm's OIDC trusted-publishing token exchange,
      # so publish with the npm CLI (>= 11.5.1) which does.
      - run: npm install -g npm@latest

      - run: npm publish --access public --provenance
